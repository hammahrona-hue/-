# TEST2.md — 压力测试：102 张 / 691 张 / 9737 张能不能"每一张都读到"

> **这是压力测试。** 目的是找出这个沙箱+模型组合的**真实上限**，不是做出成品。
> **严禁**跑 `setup.sh`、不要转写、不要 OCR、不要装包（除了解压用的 python3 标准库）。
>
> ## 三条铁律
> 1. **不许抽样冒充全量。** 要求 102 张就必须是 102 张，要求 691 张就必须是 691 张。
> 2. **做不到就说做不到**，并明确指出卡在哪一步（超上下文 / 超时 / 内存 / 其他）。
> 3. **中间结果必须立刻落盘**，不许只存在上下文里 —— 上下文爆了不能连累已完成的进度。

---

## 先搞清楚两种"读取"（这决定测试怎么做）

| 类型 | 含义 | 对 9737 张可行吗 |
|---|---|---|
| **A. 逐文件处理** | 遍历、算校验、统计 —— 脚本干，不吃 token | ✅ 完全可行 |
| **B. 视觉理解** | 模型真的"看"每一张图 —— 吃 token | ❌ 9737 张 ≈ 870 万 token，不可能 |

下面三个测试分别对应不同层次，**每个都要做，不许跳过**。

---

# 测试 1 · 一次性读完 102 张（视觉）

```bash
B=https://github.com/hammahrona-hue/-/releases/download/v1
mkdir -p /work/t && cd /work/t
curl -fL -o slides_102.zip "$B/slides_102.zip"
python3 -m zipfile -e slides_102.zip ./slides/
ls slides/*.jpg | wc -l        # 预期 102
```

**要求**：把 `slides/*.jpg` 这 **102 张一次全部读入**（不许分批、不许抽样），
对每一张输出一行，格式严格如下：

```
S001 | <标题> | <关键词1,关键词2,关键词3>
S002 | <标题> | <关键词1,关键词2,关键词3>
...
S102 | <标题> | <关键词1,关键词2,关键词3>
```

写入 `/work/t/out/vision_102.md`（先 `mkdir -p /work/t/out`）。

**判据**：
- 行数是不是 **正好 102**？
- 编号是不是 **S001 到 S102 连续无缺**？
- 有没有哪几行写成"（未读取到）"或明显是编的？

**如实回答**：这 102 张是在**一次**调用里读完的，还是你偷偷分批了？如果分批了，分了几批？

---

# 测试 2 · 691 张逐张视觉读取（分批 + 断点续跑）

```bash
curl -fL -o frames_unique_691.zip "$B/frames_unique_691.zip"
python3 -m zipfile -e frames_unique_691.zip ./uniq/
ls uniq/*.jpg | wc -l          # 预期 691
```

**要求**：**每一张都要过一遍视觉**，不许抽样。

- 分批：**每批 50 张**，共约 14 批
- 每张输出一行：`文件名 | 6~12 字的画面要点`
- **每批读完立刻 `>>` 追加写入** `/work/t/out/vision_691.tsv`
- 每批结束打印一行进度：`第 N 批完成，累计 X/691`

**判据**：
- `vision_691.tsv` 最终是不是 **691 行**？
- 里面的文件名集合，跟 `ls uniq/*.jpg` 的集合**是否完全一致**（用 `comm` 或 `sort` 对一下）？
- 中间有没有因为上下文满而丢批次？

**如实回答**：
- 你实际处理了多少张？有没有跳过？
- 在哪一批开始感觉上下文吃紧？
- 如果没做完 691 张，**卡在第几张、为什么**？

---

# 测试 3 · 9737 张逐文件核验（脚本，不是视觉）

```bash
curl -fL -o frames_all_9737.zip "$B/frames_all_9737.zip"
python3 -m zipfile -e frames_all_9737.zip ./all/
ls all/*.jpg | wc -l           # 预期 9737
```

用脚本（不是视觉）遍历**全部 9737 个文件**，输出 `/work/t/out/manifest_9737.tsv`：

```
序号 \t 文件名 \t 字节数 \t sha256前8位
```

并统计汇报：

- 文件总数
- 总字节数
- 有没有 0 字节或明显损坏的文件（有几个、哪几个）
- 最小/最大文件大小

**明确说明**：这一步是**逐文件处理**，不是"看图"。
9737 张做视觉理解需要约 **870 万 token**，超出任何模型的上下文 —— 所以**不做，也做不到**。
这不是失败，是物理限制，请如实写进结论。

---

# 最后必须回答这 6 个问题

| # | 问题 | 答案 |
|---|---|---|
| 1 | 102 张能否**一次**读完？行数够不够？ | |
| 2 | 691 张能否**逐张**读完？实际处理了几张？ | |
| 3 | 691 张你分了几批？中间有没有丢？ | |
| 4 | 9737 张能否**逐文件核验**？总数对不对？ | |
| 5 | 9737 张能否**逐张视觉理解**？为什么？ | |
| 6 | 本次测试你大概消耗了多少 token / 花了多久？ | |

**最后一句话必须是这样格式的**：

> 我能一次读完 102 张：能/不能。
> 我能逐张视觉读完 691 张：能/不能，实际完成 __ 张。
> 我能逐文件核验 9737 张：能/不能。
> 我能逐张视觉理解 9737 张：**不能**（原因：____）。
