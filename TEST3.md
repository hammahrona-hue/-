# TEST3.md — 定位测试：找出"一次能看几张图"的真实上限

> **目的**：区分「模型不支持视觉」还是「工具链不支持批量注入」。
> 这两个问题的解法完全不同 —— 前者要换模型，后者换模型也没用。
>
> **只做读取，不要跑 setup.sh、不要转写、不要 OCR、不要装包。**
> **不许编造**：看不见就说看不见。

---

## 准备（只下小包，10 MB）

```bash
B=https://github.com/hammahrona-hue/-/releases/download/v1
mkdir -p /work/t3 && cd /work/t3
curl -fL -o slides_102.zip "$B/slides_102.zip"
python3 -m zipfile -e slides_102.zip ./slides/
ls slides/*.jpg | wc -l        # 预期 102
mkdir -p out
```

---

## 递进测试：从 1 张开始，逐级加码

每一级都**只用一次请求**把该数量的图交给模型，然后回答该级的问题。
**某级失败就停下来**，不要跳到更高级别，也不要重试超过 1 次。

### 第 1 级 · 1 张
把 `slides/S001.jpg` 交给模型。
**回答**：这张图的标题是什么？右下角的数字水印是多少？

### 第 2 级 · 3 张
一次交出 `S001.jpg`、`S002.jpg`、`S003.jpg`。
**回答**：三张的标题分别是什么？（必须三张都答）

### 第 3 级 · 5 张
一次交出 `S001.jpg` ~ `S005.jpg`。
**回答**：五张的标题，按顺序列出。

### 第 4 级 · 10 张
一次交出 `S001.jpg` ~ `S010.jpg`。
**回答**：十张的标题，按顺序列出。

### 第 5 级 · 20 张
一次交出 `S001.jpg` ~ `S020.jpg`。
**回答**：二十张的标题，按顺序列出。

### 第 6 级 · 50 张
一次交出 `S001.jpg` ~ `S050.jpg`。
**回答**：只报告你**实际看到了**几张；哪几张没看到就明确说"没看到"。

### 第 7 级 · 102 张
一次交出全部 102 张。
**回答**：同上，如实报告。

---

## 关键要求

- **每一级都要记录**：成功 / 失败、模型实际"看到"了几张、报错或限制提示的**原文**
- **不许用文件名或 OCR 文本推断标题** —— 那不算看见
- 某级失败后，把该级的**原始报错/限制信息**贴出来（这是最关键的证据）
- 把各级结果写入 `/work/t3/out/vision_limit.md`

---

## 最后必须回答这 5 个问题

| # | 问题 | 答案 |
|---|---|---|
| 1 | 一次能看 **1** 张吗？ | |
| 2 | 一次能看 **5** 张吗？ | |
| 3 | 一次能看 **10** 张吗？ | |
| 4 | 一次能看 **20** 张吗？ | |
| 5 | 失败那一级的**原始报错/限制提示**是什么？ | |

**最后一句话，必须二选一：**

> A. 我的限制来自**模型**（模型本身没有视觉能力）—— 证据：____
>
> B. 我的限制来自**工具链**（模型能看图，但工具不支持一次送多张）—— 证据：____
