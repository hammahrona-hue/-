# TASK.md — 制作一份「更好懂、更高效」的学习资料

> ## 🔴 阅读顺序（必须按这个顺序）
> 1. **`REQUIREMENTS.md`** ← **先读这份！考生本人的原始要求，优先级最高**
> 2. 本文件 `TASK.md` —— 素材在哪、每个文件是什么、怎么读
> 3. `BRIEF.md` —— 制作规格（通俗化标准 / 覆盖度 / 设计语言 / 分块产出）
> 4. `CLAUDE.md` —— 沙箱作业指导（路径、参数、push 方式）
>
> **任何执行方式与 `REQUIREMENTS.md` 冲突时，以 `REQUIREMENTS.md` 为准。**

---

## 0. 背景

考生要参加 **2026 年下半年四川教师编制考试**（教育公共基础）。
他反复说过同一件事：**「心理学太晦涩了，我听不懂」「我看着累，越看越不想学」。**

已经做过一版（见 `out/` 里的两份 HTML），但考生反馈：
- 语言还是不够通俗，**希望「把我当小学生讲」**
- 希望有**更强的画面感、更少的文字墙**
- 希望**看得下去、看得进去**

**你的任务：在现有素材基础上，做一份真正让他看得懂、愿意看的学习资料。**

---

## 1. 素材清单（都在 `out/` 目录）

### 1.1 文字素材（**这是重点，先全部读完**）

| 文件 | 大小 | 是什么 | 怎么用 |
|---|---|---|---|
| `out/transcript.txt` | 126 KB | **全片文字稿 45,541 字**，老师讲的每一句话（带 `[mm:ss]` 时间戳） | ⭐ **真相源**。所有知识点的最终依据 |
| `out/slides_unique.txt` | 69 KB | **102 张幻灯片的 OCR 文本**，`=== S### [mm:ss] ===` 分节 | 幻灯片上印了什么 |
| `out/ocr_all.jsonl` | 566 KB | **691 帧逐帧 OCR**，每行 `{idx,file,t,g_start,g_end,text}` | 细粒度对照；`g_start/g_end` 是该画面覆盖的时间段 |
| `out/slides_index.md` | 255 KB | **691 行「画面 ↔ 讲解」对照表** | 想知道「这句话配哪张图」就查它 |
| `out/slides_index.json` | 815 KB | 同上，结构化版本 | 程序化处理用 |
| `out/slides/manifest.md` | 43 KB | 102 张唯一幻灯片的清单（编号/时间/OCR） | 索引 |
| `out/uniq_meta.json` | 58 KB | 帧时间轴元数据 | 一般不用 |
| `out/asr_raw.jsonl` | 142 KB | 转写原始结果（未清洗） | 一般不用 |

### 1.2 现有成品（**参考用，不要照抄**）

| 文件 | 说明 |
|---|---|
| `out/心理学12_精讲全解.html` | 3 幕 16 节，逐点精讲 |
| `out/心理学12_闯关记忆手册.html` | 22 关闯关 + 擂台 + 错题本 + 闪卡 |
| `out/幻灯片总览_高清版.html` | 102 张幻灯片 + OCR + 讲解 |
| `out/幻灯片总览.html` | 691 帧（缩略图很小，只用它的文本） |

> ⚠️ **这三个 HTML 里内嵌了大量 base64 图片，直接读会浪费巨量 token。**
> **读之前先剥离 base64**：
> ```bash
> python3 - <<'EOF'
> import re,os
> for f in ['心理学12_精讲全解.html','心理学12_闯关记忆手册.html','幻灯片总览_高清版.html','幻灯片总览.html']:
>     p='out/'+f
>     if not os.path.exists(p): continue
>     h=open(p,encoding='utf-8').read()
>     h=re.sub(r'data:image/jpeg;base64,[A-Za-z0-9+/=]+','[图]',h)
>     open('/tmp/'+f+'.txt','w',encoding='utf-8').write(h)
>     print(f, '->', len(h), '字符')
> EOF
> ```
> 然后读 `/tmp/*.txt`。

### 1.3 图片（**重要限制，先看这条**）

幻灯片原图在 Release 附件里：
- `slides_102.zip`（10 MB）— **102 张唯一幻灯片**，命名 `S001.jpg` ~ `S102.jpg`
- `frames_unique_691.zip`（76 MB）— 691 张代表帧
- `frames_all_9737.zip`（729 MB）— 全部 9737 帧

> ❌ **本沙箱的视觉工具一次只能读一张图，无法批量注入。**
> 所以**不要试图「读完 102 张图」** —— 已经实测过，做不到。
>
> ✅ **正确做法**：以 `transcript.txt` + `slides_unique.txt` + `ocr_all.jsonl` 为准
> （它们已经覆盖了幻灯片上的全部印刷内容）；
> 需要看某张图时，单独下载 `slides_102.zip`，**一张一张**读（一次一张，别贪多）。

---

## 2. 这一讲讲的是什么（范围）

**《心理学12》= 「学习与学习理论」这一章的前半部分。** 老师实际讲到：

1. **学习分类** —— 奥苏贝尔（两个维度）· 加涅（学习结果 / 学习水平）· 阿瑟雷伯（内隐外显）· 冯忠良 / 潘菽
2. **四大流派总览** —— 行为 / 认知 / 建构 / 人本
3. **行为主义（重点，考频 76 次）**
   - 概述：华生、小阿尔伯特实验、"第一革命第一势力"
   - **桑代克**：试误说、猫开笼取食实验、三条学习公律（准备律 / 练习律 / 效果律）
   - **巴甫洛夫**：经典性条件作用、狗进食摇铃实验、无条件/条件反射、第一/第二信号系统、
     一级 vs 高级条件作用、获得 / 消退 / 泛化 / 分化
   - 溯源拔高：经典性（S→R）vs 操作性（R→S）

**没讲到**（老师原话"明天讲"）：斯金纳、班杜拉、认知主义、建构主义、人本主义。
**别去编这部分内容** —— 没讲就是没讲，不要自己加。

---

## 3. 要做什么

> ⚠️ **要达成什么目标、验收标准是什么，全部在 `REQUIREMENTS.md` 第三节里，请回去读。**
> 本节不再重复，也**不规定方法**。

**一句话**：做一份比现有版本**更好懂、更愿意看、更记得住**的学习资料。

**形式、结构、载体全部由你决定。** 唯一的要求是满足 `REQUIREMENTS.md` 第三节的六条验收标准
（覆盖度 / 通俗度 / 记忆度 / 做题度 / 体验度 / 形式）。

**这里只补一条环境提示**（不是方法，是事实）：
本讲范围 = **学习分类 + 四大流派总览 + 行为主义（华生 / 桑代克 / 巴甫洛夫）+ 溯源拔高**。
**斯金纳、班杜拉、认知主义、建构主义、人本主义老师没讲**（原话"明天讲"）——
**没讲就是没讲，不要自己补内容。**

---

## 4. 交付

```bash
git add -A && git commit -m "feat: 心理学12 新版学习资料"
git push -u origin HEAD     # 必须带 -u origin HEAD
```

**汇报时必须包含**（这几项是 `REQUIREMENTS.md` 要求的，不是建议）：

1. 你做了什么（形式、结构、你的设计思路）
2. **覆盖度数字**：知识点总数 / 已覆盖 / 未覆盖
3. 与旧版相比，你在"更好懂"和"更记得住"上具体改了什么
4. 遇到的限制或没做完的部分（**如实说，不要编**）
